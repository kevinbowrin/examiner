
builds: 
  -
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - 386
      - amd64
archive:
  replacements:
    darwin: macOS
  wrap_in_directory: true
  format_overrides:
    - goos: windows
      format: zip
  files:
    - LICENSE 
    - gtfs.db
    - rebuild_gtfs_db.sh
checksum:
  name_template: 'checksums.txt'
changelog:
  filters:
    exclude:
    - '^docs:'
    - '^test:'
